# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
SYMFONY_DEPRECATIONS_HELPER=999999
PANTHER_APP_ENV=panther
PANTHER_ERROR_SCREENSHOT_DIR=./var/error-screenshots

# Use synchronous messenger for tests
MESSENGER_TRANSPORT_DSN=sync://

# Use flock or semaphore for locking in tests (simpler, no redis dep)
LOCK_DSN=flock

# Redis URL (we still use it for cache/rate limiter if configured, but locking/messenger use separate)
# Using separate redis DB for tests if needed, or mock.
# But for now let's stick to what allows tests to pass.
